git cloning/change branch name /

Using already existing repo

clone repository to different directory

GIT GLOBAL/SYSTEM/LOCAL
git config --global user.name "User_Name" 
git config --global user.email "your_email@example.com"
$ git branch // to get the name of the repository we are working
$ git add *(.)// to add every changes 
$ git add name of the file // to add perticular file
$ git status // to get to know the files added after the commit
$ git status -s // only changes 
$ git commit -m"write what ever you want"
$ ls .git// get the header,index
$ cat (start.txt)file name // details of the file
ls -a// .git contain then the repository created
cd //change directory
pwd // present working directory
cd directory //it will move the current directory
$ git config --list //to check user name& MAIL
$ git config --list --show-origin//
$ git --help status(name of the command)
$ git config --global core.editor gedit // to create editor
$git config --global --unset core.editor// to remove editor config
$git config --global/system/local --unset (name)// to remove ==> local highest global next last system
$ git (name of the command) --help // to know about the coomand
$ git init //Initialized empty Git repository
$ git config --global init.defaultBranch main// to change the default branch name
$ git commit// to apply changes 
$ vi .gitignore enter ==write the files you don't need to shows/ you remove the files that are added to ignore =
$ git restore --staged name // to revert back the tracked file to untracked
$ cat .gitignore // to know the filed that are ignored
$ git diff // get all the changes  done in the files after commit to latest untracked files
$ git diff (file name)// only the required changed on the file
(commit had id we used with git diff commit first 6numbers and another commit (old ) 6 digits we get )
$ git log // get the commits done on the repository
$ git commit -a -m"matter"// to add files to staged & commit in one go(aft original onced staged only it work)
$ git restore --staged * // we can go back to the staged to the working directory
$ rm file name // remove it will go back to the staged
$ git log -p -2//(patch ) show the difference between the 2 commits
$ git log --oneline//
$ git log --oneline --graph // in one line
$ git log --since=20.hours(week/day)// to get required time and date
$ git log --pretty=format:"%h - %an, %ar : %s"// give the all commits done latest to the old
$ git branch// to get the branch details
$ git log --oneline (branch name )// to get the commits done on that branch
===UNDOING CHANGES====
$ git commit --amend -a -m  // ( it can modified the top most commit - did not show the log details of the last before one)-it is only done to the commits not push to the server
$ git reset --soft HEAD~1//( it will be change the commit to the last before one header will change)
$ git reset --hard HEAD~1//( it will back  history of log & data change)
$ git reset--$ git reset head one.txt // stagged to back to working directory
$ git restore --staged one.txt(file name)//back to working directory
$ git restore one.txt==$ git checkout one.txt (name of the file name)//untraced to delete
$ git tag -a "name version" 7bf8969(log)// to create annotated tag
$ git show 7bf8969(name of the tag)// to show the details of everything include user name and mail
$ git tag // show the tag present are not
$ git tag "v1.0.3" 0b61733(log)==to create light weighted tag
$ git tag -l v1.0 * // to give the similar name tags
$ git tag --d 7bf8969 // deleting the tag
$ git checkout -b  name of the branch name of the tag // create a tag to the branch
$ git config --global alias.ci(short cut) commit(command)// create short cut for commands like:commit/checkout














 

  
 
 


























































open git hub open branch from the main
git clone path url- to clone the code from the git
git branch -a /to check in which branch we are in
git pull origin main(working branch) /to take the latest code from the main branch
git status / to see changed files
git stash/ for stashing changes 
git fetch/ to pull new branches created in git hub repository to local repositary
git checkout branch name/ to switch to feature branch 
git stash pop / to get stashed changes 
git add file path/ before commiting we need to add files for this adding purpose we use git add
git commit -m  "commit message for your changes"
after this changes are pushed to in your local 
git pull origin main(base branch)/ to sink your feature branch with main branch
git push to push your local changes to git hub - after that we need to raise pull request & add reviewers to the list
  
before raising pull request we should check changed files if any unwanted changes are present we need to re commit and push

 
